{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core APIs for the launcher and editor windows. Instance windows are deliberately left out: they render untrusted remote content and must not reach the Tauri API.",
  "windows": ["main", "themes"],
  "permissions": ["core:default"]
}
